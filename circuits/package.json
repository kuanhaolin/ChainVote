{
  "name": "chainvote-zkp",
  "version": "1.0.0",
  "description": "ZKP circuits for anonymous voting",
  "scripts": {
    "compile": "circom circuits/vote.circom --r1cs --wasm --sym --c -o build",
    "setup": "snarkjs groth16 setup build/vote.r1cs pot/powersOfTau28_hez_final_20.ptau build/vote_0000.zkey",
    "contribute": "snarkjs zkey contribute build/vote_0000.zkey build/vote_final.zkey --name='Contribution' -v",
    "export-vkey": "snarkjs zkey export verificationkey build/vote_final.zkey build/verification_key.json",
    "export-verifier": "snarkjs zkey export solidityverifier build/vote_final.zkey chain/VoteVerifier.sol",
    "gen-proof": "node scripts/generateProof.js",
    "verify-proof": "snarkjs groth16 verify build/verification_key.json build/public.json build/proof.json"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.5",
    "ffjavascript": "^0.3.0",
    "circomlibjs": "^0.1.7"
  }
}
